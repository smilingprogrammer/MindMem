# `main.py`

## Purpose

Runs three predefined messages through text input creation and lightweight
relevance scoring. It is a small manual demonstration, not library code.

## Module execution

The top-level loop creates a `TextInputEvent`, calls `score_relevance()`, and prints
the score, label, and detected signals. It exists to visualize heuristic behavior.
